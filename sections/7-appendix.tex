\chapter{Appendix}
\label{chap:appendix}

%% ──────────────────────────────────────────────────────────────────────────
\section{Hardware and Software Environment}
\label{app:env}

All experiments were run on Snellius (project EINF-16254) on a single node with $2 \times $ NVIDIA H100 80\,GB GPUs. The software stack is PyTorch 2.5 with CUDA 12.4, mixed-precision bf16, FlashAttention-2, and PyTorch Distributed (NCCL) for multi-GPU training. Each Stage~1 run takes approximately 36--48 hours wall-clock; the Stage~2 runs take an additional 12--24 hours per variant.

%% ──────────────────────────────────────────────────────────────────────────
\section{Per-Variant Configuration Flags}
\label{app:flags}

For reproducibility, Table~\ref{tab:app_flags} lists the exact command-line flags passed to each of the seven variants. The training script is \texttt{train\_can3tok\_2.py} in the released codebase, with the YAML config \texttt{configs/scenesplat7k.yaml} providing the architecture defaults.

\begin{table*}[t]
  \centering
  \caption[Per-variant SLURM job ids and flags]{\textbf{SLURM job identifiers and key flags for the seven Stage~1 variants.} The full job scripts are in the released repository.}
  \label{tab:app_flags}
  {\footnotesize
  \begin{tabular}{p{0.06\linewidth}p{0.10\linewidth}p{0.76\linewidth}}
    \toprule
    Variant & SLURM Job & Key flags \\
    \midrule
    V1 & 23970540 &
      \texttt{--scaffold\_mode voxel} \\[4pt]
    V2 & 23971383 &
      \texttt{--morton\_order --order\_curve hilbert --scaffold\_mode voxel} \\[4pt]
    V3 & (separate) &
      \texttt{--morton\_order --position\_scaffold --scaffold\_mode hilbert\_block}
      \texttt{--anchor\_loss\_weight 1.0} \\[4pt]
    V4 & 23973796 &
      \texttt{--token\_local\_decoder --scaffold\_mode hilbert\_block} \\[4pt]
    V5 & 23976584 &
      \texttt{--token\_local\_decoder --local\_encoder --structured\_latent}
      \texttt{--scaffold\_mode hilbert\_block} \\[4pt]
    V6 & 23978168 &
      \texttt{--local\_disentangle --token\_local\_decoder --local\_encoder}
      \texttt{--position\_scaffold --scaffold\_mode hilbert\_block --morton\_order}
      \texttt{--semantic\_mode hidden --scene\_semantic\_head --scene\_layout\_head}
      \texttt{--structured\_layout\_tokens --segment\_loss\_weight 0.4} \\[4pt]
    V7 & 23978200 &
      As V6 but without \texttt{--position\_scaffold} \\
    \bottomrule
  \end{tabular}}
\end{table*}

%% ──────────────────────────────────────────────────────────────────────────
\section{Hyperparameter Defaults}
\label{app:hparams}

Table~\ref{tab:app_hp} lists hyperparameters not already stated in \S\ref{sec:method_configs}. The optimiser, learning-rate schedule, and architecture widths are reported in the main text; only the secondary loss weights and the smaller architectural numbers appear here.

\begin{table}[h]
  \centering
  \caption[Common Stage~1 hyperparameters]{\textbf{Common Stage~1 hyperparameters not already in \S\ref{sec:method_configs}.}}
  \label{tab:app_hp}
  {\small
  \begin{tabular}{ll}
    \toprule
    Hyperparameter                  & Value \\
    \midrule
    Colour loss weight              & 2.0 \\
    Mean colour loss weight         & 0.5 \\
    Anchor loss weight (scaffolded) & 1.0 \\
    Layout latent tokens (V6/V7)    & 16 \\
    Local encoder window $\omega$   & 1 \\
    Fourier feature frequencies     & 8 \\
    Gaussians per scene $N$         & 40{,}000 \\
    InfoNCE temperature (V6/V7)     & 0.07 \\
    Max epochs                      & 500 \\
    Validation interval             & 10 epochs \\
    \bottomrule
  \end{tabular}}
\end{table}

%% ──────────────────────────────────────────────────────────────────────────
\section{Validation Convergence by Variant}
\label{app:epoch_tables}

Validation runs every 10 epochs on the 100 held-out full scenes ($\text{val}^{\text{full}}$) and the 88 held-out chunks ($\text{val}^{\text{chunk}}$). Table~\ref{tab:app_epochs} compiles the $\text{val}^{\text{full}}\,\ell_2$ trajectory for all seven variants at a shared set of checkpoints, plus the final-epoch number. Best entries per variant are in bold. Per-10-epoch numbers and the chunk-validation series are in the released training logs.

\begin{table*}[t]
  \centering
  \caption[Combined per-variant validation progression]{\textbf{Validation $\ell_2$ on the 100 held-out full scenes across training.} One row per checkpoint epoch, one column per variant. A dash indicates the variant had already finished or had not yet reached that epoch. Bold marks the best (lowest) value reached by each variant.}
  \label{tab:app_epochs}
  {\small
  \begin{tabular}{rccccccc}
    \toprule
    Epoch & V1 & V2 & V3 & V4 & V5 & V6 & V7 \\
    \midrule
    10    & 113.4 & 88.2 & 60.3 & 110.2 & 95.2 & 62.4 & 63.7 \\
    30    & 102.7 & 79.5 & 49.9 & 100.9 & --   & 53.7 & 54.8 \\
    60    & 98.8  & 77.2 & 45.8 & 99.1  & 87.3 & 49.8 & 51.1 \\
    90    & 99.4  & 77.4 & \textbf{44.63} & 100.2 & --   & 48.4 & 50.0 \\
    120   & 100.1 & 78.2 & 45.0 & 101.4 & --   & 47.9 & 49.5 \\
    160   & --    & --   & --   & --    & 82.0 & 47.6 & 49.1 \\
    200   & --    & --   & --   & --    & 81.2 & \textbf{47.48} & 48.96 \\
    230   & --    & --   & --   & --    & --   & --   & \textbf{48.89} \\
    300   & 106.0 & 81.2 & 46.7 & 106.5 & 80.0 & --   & -- \\
    410   & --    & --   & --   & --    & \textbf{79.11} & --   & -- \\
    \midrule
    \emph{final} & 107.79 & 81.71 & 47.07 & 107.79 & 79.97 & 49.10 & 50.20 \\
    \bottomrule
  \end{tabular}}
\end{table*}

%% ──────────────────────────────────────────────────────────────────────────
\section{300-Scene Configurations}
\label{app:300_configs}

The 300-scene runs used a fixed split of 180/60/60 train/val/test scenes from the chunked ScanNet subset of SceneSplat-7K, batch size 16, and 200 training epochs. Hyperparameters otherwise match the large-scale runs (Table~\ref{tab:app_hp}). The four disentanglement strategies (A, B1, B2, C) and the three InfoNCE flavours (pgNCE, PoolNCE, Pool+pgNCE) are toggled by command-line flags as described in the released codebase.

%% ──────────────────────────────────────────────────────────────────────────
\section{Reproducibility Notes}
\label{app:repro}

\paragraph{Dataset locations.} Preprocessed scenes are under \texttt{/gpfs/work1/0/prjs2084/Hafeez\_thesis/Semantic-Scene-Completion/data/} with the following structure: \texttt{train/} (3{,}800 chunks), \texttt{interior\_gs/} (800 full scenes), \texttt{arkitscenes/}, \texttt{scannetpp/}, \texttt{val\_full/} (100 held-out full scenes), \texttt{val\_chunks/} (88 held-out chunks).

\paragraph{Seeds.} Each run uses a fixed seed of 42 for the PyTorch RNG and a fixed seed of 0 for the NumPy RNG used in the dataset shuffling. The Hilbert key computation is deterministic.

\paragraph{Logging.} Training logs (per-batch loss, per-epoch validation) are written to \texttt{logs/<JOB\_ID>.out}. Checkpoints are saved every 10 epochs to \texttt{checkpoints/<run\_name>/}.