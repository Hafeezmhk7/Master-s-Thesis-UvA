\chapter{Results}
\label{chap:results}

This chapter reports the empirical findings. I begin with the 300-scene exploration that motivated the disentangled latent and the per-Gaussian semantic supervision (\S\ref{sec:results_300}), then move to the seven-variant large-scale ablation that is the main quantitative contribution (\S\ref{sec:results_7}). Per-component loss breakdowns and validation convergence are reported in \S\ref{sec:results_components} and \S\ref{sec:results_convergence}. Qualitative Stage~1 reconstructions and the per-Gaussian PCA visualisations are in \S\ref{sec:results_quality} and \S\ref{sec:results_pca}. Stage~2 results are in \S\ref{sec:results_stage2}.

Throughout this chapter the validation metric for Stage~1 is the same element-wise $\ell_2$ residual on the 14-dim Gaussian vector that is used in training (Eq.~\eqref{eq:recon_noscaffold}). Two validation sets are reported, the 100 held-out full scenes ($\text{val}^{\text{full}}$, the primary thesis-target metric) and the 88 held-out chunks ($\text{val}^{\text{chunk}}$, an in-distribution diagnostic). For Stage~2 the metric is the rectified-flow velocity MSE (Eq.~\eqref{eq:flow_matching}) on the corresponding latent. All numbers are read directly from the SLURM job logs.

%% ──────────────────────────────────────────────────────────────────────────
\section{300-Scene Exploratory Study}
\label{sec:results_300}

\paragraph{Setup.} I subsampled 300 scenes from the chunked ScanNet split (180 train, 60 val, 60 held-out test for swap diagnostics) and ran a sweep over the three latent-structure strategies described in \S\ref{sec:method_semantic} and the three InfoNCE flavours. All other hyperparameters match the eventual large-scale configuration, except for batch size 16 and 200 epochs.

\paragraph{Reconstruction is stable across variants.} At 300 scenes the network has enough capacity to fit any of the four variants, so the raw reconstruction quality is similar across the sweep. The validation $\ell_2$ at epoch 200 sits in the 35--40 range across all four variants. This is consistent with the failure mode at scale being one of generalisation, not of representational capacity.

\paragraph{Layout-vs-geometry stability.} A more diagnostic metric is the \emph{stability} of $z_s$ under partial-scan completion, defined as the cosine similarity between $z_s$ produced from a full scene and $z_s$ from a 40\%-randomly-masked partial version. A high value (close to 1) indicates the layout component is stable under missing data, which is the property the disentanglement is meant to enforce. Table~\ref{tab:300_disentangle} gives the comparison.

\begin{table}[h]
  \centering
  \caption[300-scene latent disentanglement comparison]{\textbf{Layout stability at 300 scenes.} Higher Stab.@40\% is better. Strategy~B1 is the deterministic-layout variant and gives the most stable $z_s$ under masking; Strategy~A is the VAE-sampled variant; Strategy~B2 (additive bias) failed to disentangle and is shown for completeness; Strategy~C (no disentanglement) has no $z_s$ to measure.}
  \label{tab:300_disentangle}
  {\small
  \begin{tabular}{lcc}
    \toprule
                     & Val.\ recon.\ $\ell_2$ ($\downarrow$) & Stab.@40\% ($\uparrow$) \\
    \midrule
    Strategy~A       & 36.1 & 0.62 \\
    Strategy~B1      & 35.9 & \textbf{0.76} \\
    Strategy~B2      & 36.4 & 0.31 \\
    Strategy~C       & 35.7 & --   \\
    \bottomrule
  \end{tabular}}
\end{table}

Strategy~B1 wins on stability by a comfortable margin while matching the others on raw reconstruction. The deterministic layout code is what gives it this property: the same partial scene routes through the layout projector to the same code regardless of the random KL noise added downstream. Strategy~B2 confirms that an additive layout bias is absorbed by the decoder as a constant offset and gives no useful conditioning.

\paragraph{Per-Gaussian InfoNCE separates categories.} I use a swap diagnostic: take a held-out test scene, generate its $z_s$, replace the bottom-$p$ per cent of Gaussians by category-shuffled samples drawn from other scenes, and measure how many of the swapped Gaussians are still classified as the original category by a simple kNN over the per-Gaussian feature. Higher is better, since it means the swap is invisible at the feature level. Table~\ref{tab:300_nce} shows that combining PoolNCE on $z_s$ with pgNCE on per-Gaussian features (Pool+pgNCE) is the strongest combination at the same reconstruction cost.

\begin{table}[h]
  \centering
  \caption[300-scene InfoNCE comparison]{\textbf{Per-Gaussian feature quality at 300 scenes.} Cross-scene swap test (lower P1-of-swap means category-aware features). Pool+pgNCE reduces the swap from 6.5\% to 0.08\% while matching reconstruction; pgNCE alone gives an intermediate value.}
  \label{tab:300_nce}
  {\small
  \begin{tabular}{lcc}
    \toprule
                       & Val.\ recon.\ $\ell_2$ ($\downarrow$) & \%P1 swap ($\downarrow$) \\
    \midrule
    No NCE             & 35.7 & 6.50 \\
    pgNCE              & 35.8 & 0.41 \\
    PoolNCE            & 35.9 & 1.20 \\
    Pool+pgNCE         & 35.9 & \textbf{0.08} \\
    \bottomrule
  \end{tabular}}
\end{table}

Variants 6 and 7 in the large-scale ablation use Strategy~B1's deterministic layout idea, together with the layout-vs-geometry KL split described in \S\ref{sec:method_disentangle} and Pool+pgNCE-style supervision. None of these is the literal small-scale code; they are the same ideas scaled up. The takeaway from the 300-scene study is that a layout/geometry split is a viable structural choice and per-Gaussian semantic supervision adds category structure without degrading reconstruction. Both findings carry over.

%% ──────────────────────────────────────────────────────────────────────────
\section{Seven-Variant Large-Scale Ablation}
\label{sec:results_7}

This section is the main quantitative result of the thesis. The seven configurations of Table~\ref{tab:configs} are trained on the same SceneSplat-7K-derived corpus described in \S\ref{sec:method_dataset}. Validation is on the 100 held-out full scenes and the 88 held-out chunks. All numbers below are read from the SLURM job logs.

\paragraph{Headline numbers.} Table~\ref{tab:7_summary} summarises the best validation reconstruction across training and the corresponding epoch. Two patterns are immediately visible: adding Hilbert reorder plus the position scaffold (V1 $\to$ V2 $\to$ V3) roughly halves the validation $\ell_2$, and the token-local decoder reduces the train-test gap by an even larger factor when paired with the local encoder.

\begin{table*}[t]
  \centering
  \caption[Headline numbers for the seven-variant ablation]{\textbf{Best validation reconstruction across the seven Stage~1 variants.} Lower is better. Best epoch indicates when the minimum was observed. ``Final'' columns report the last logged epoch's metrics.}
  \label{tab:7_summary}
  {\small
  \begin{tabular}{lcccccc}
    \toprule
        & Best $\text{val}^{\text{full}}$ $\ell_2$ ($\downarrow$) & Best epoch & Best $\text{val}^{\text{chunk}}$ $\ell_2$ ($\downarrow$) & Final $\text{val}^{\text{full}}$ & Final $\text{val}^{\text{chunk}}$ & Status \\
    \midrule
    V1  & 98.75  & 60  & 85.86 & 107.79 & 91.70 & finished \\
    V2  & 76.74  & 70  & 80.85 & 81.71  & 75.42 & finished \\
    V3  & 44.63  & 90  & 49.20 & 47.07  & 46.75 & finished \\
    V4  & 98.53  & 40  & 85.58 & 107.79 & 94.11 & finished \\
    V5  & 79.11  & 410 & 84.16 & 79.97  & 84.43 & ckpt @ ep 410 \\
    V6  & 47.48  & 200 & 51.10 & 49.10  & 52.30 & finished \\
    V7  & 48.89  & 230 & 53.40 & 50.20  & 54.10 & finished \\
    \bottomrule
  \end{tabular}}
\end{table*}

\paragraph{What each row tells us.} I read Table~\ref{tab:7_summary} in three slices.

\emph{Slice 1: ordering alone.} V1 (no ordering, flat decoder) is the un-modified Can3Tok baseline. Validation $\ell_2$ plateaus around 99 with no further improvement after epoch 60. V2 adds the Hilbert reorder; the same architecture, same parameter count, same training schedule, now reaches 77, a 22-point absolute drop. This is the cleanest single-knob result in the thesis: imposing a canonical order on the target makes the same network learn substantially better.

\emph{Slice 2: ordering plus position scaffold.} V3 adds anchor-relative position decoding on top of Hilbert ordering. Validation drops from 77 to 45, almost another factor of two. The position component of the loss does most of the heavy lifting (\S\ref{sec:results_components}); the scaffold gives the decoder a meaningful local origin to predict against.

\emph{Slice 3: architectural change.} V4 isolates the token-local decoder on top of the baseline (no Hilbert reorder, no scaffold, no local encoder). It does not help by itself; validation lands at 99, essentially the same as V1. The mistake here would be to conclude that the token-local decoder is useless. V5 adds the local encoder and the structured latent in step. Validation now drops to 79, very close to V2 (which used a 777M-parameter flat decoder), but at $\sim 1.6$M decoder parameters, a $\sim 500\times$ reduction. The token-local decoder needs the local encoder to feed it the right kind of context: with a global encoder, every token is a global mixture and the per-token MLP has no spatially-meaningful target. The two components are interlocking.

\emph{Slice 4: the full system.} V6 and V7 combine all of the above with the disentangled latent and per-Gaussian InfoNCE. V6 reaches 47.5 (the position scaffold is on), V7 reaches 48.9 (the position scaffold is off). The two are statistically close in the aggregate metric; the V6 latent has the position-scaffold structure that the Stage~2 hierarchical generator needs.

%% ──────────────────────────────────────────────────────────────────────────
\section{Per-Component Loss Breakdown}
\label{sec:results_components}

The aggregate $\ell_2$ in Table~\ref{tab:7_summary} hides where the improvement comes from. Table~\ref{tab:components} gives the per-attribute breakdown at the best validation epoch of each finished or sufficiently advanced run.

\begin{table*}[t]
  \centering
  \caption[Per-attribute loss breakdown]{\textbf{Per-attribute validation loss on the 100 held-out full scenes.} Position ($\vmu$), colour ($c$), opacity ($\alpha$), scale ($s$), and quaternion ($q$). All components are element-wise $\ell_2$, summed across the per-attribute dimension. The position component is by far the dominant one in the early runs and is what the Hilbert order and anchor scaffold target. Opacity and quaternion are nearly saturated even in V1 (the geometry is dense and the orientation has limited variation in indoor scenes), so the absolute improvements there are small.}
  \label{tab:components}
  {\small
  \begin{tabular}{lccccc}
    \toprule
        & $\vmu$ ($\downarrow$) & $c$ ($\downarrow$) & $\alpha$ ($\downarrow$) & $s$ ($\downarrow$) & $q$ ($\downarrow$) \\
    \midrule
    V1  & 88.04 & 4.21 & 0.81 & 4.32 & 1.37 \\
    V2  & 66.10 & 4.10 & 0.80 & 4.30 & 1.44 \\
    V3  & 34.05 & 4.18 & 0.78 & 4.25 & 1.37 \\
    V4  & 87.79 & 4.31 & 0.80 & 4.31 & 1.32 \\
    V5  & 68.31 & 4.05 & 0.79 & 4.27 & 1.69 \\
    V6  & 36.78 & 4.20 & 0.78 & 4.31 & 1.41 \\
    V7  & 38.10 & 4.22 & 0.78 & 4.31 & 1.48 \\
    \bottomrule
  \end{tabular}}
\end{table*}

Position is the dominant component, and it is also the component most directly targeted by the Hilbert reorder and the anchor scaffold. The drop from 88 (V1) to 34 (V3) on the position component matches a similarly steep drop in the aggregate. Colour, opacity, scale, and quaternion are nearly constant across variants. Two consequences:
\begin{itemize}
\item The hard part of scaling Stage~1 is the position prediction. Everything else is roughly solved at all scales the dataset reaches.
\item The DC/AC colour split (\S\ref{sec:method_colour}) is doing its job. Even in V1, where the rest of the system fails, colour reconstruction is already nearly saturated.
\end{itemize}

%% ──────────────────────────────────────────────────────────────────────────
\section{Validation Convergence}
\label{sec:results_convergence}

The validation trajectories of the seven variants are tabulated in full in Appendix~\ref{app:epoch_tables}. The most informative observations are how early V1 and V4 plateau (around epoch 50 they stop improving on the full-scene validation), the late-training drift of V5 (which keeps improving slowly until epoch 410, the latest checkpoint), and the smooth V6/V7 trajectories that hit their minima between epochs 200 and 230 and then plateau.

%% ──────────────────────────────────────────────────────────────────────────
\section{Qualitative Stage 1 Reconstruction}
\label{sec:results_quality}

The aggregate $\ell_2$ in Table~\ref{tab:7_summary} is informative but does not convey what each variant actually produces. Figure~\ref{fig:recon} renders the ground truth scene and the reconstructions from four variants (V1, V3, V6, V7) on the same held-out gym scene, viewed from the same top-down camera in SuperSplat. I am deliberate about the variants chosen: V1 is the un-modified baseline, V3 is the best run before the architectural change, V6 is the full system with the position scaffold, and V7 removes the scaffold to isolate its effect on the disentangled latent.

\begin{figure*}[t]
  \centering
  \begin{subfigure}[t]{0.19\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/gt.png}
    \caption*{(a) Ground truth}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.19\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R1_reconstructed.png}
    \caption*{(b) V1 baseline}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.19\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R3_reconstructed.png}
    \caption*{(c) V3 (+ Hilbert + scaffold)}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.19\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R6_reconstructed.png}
    \caption*{(d) V6 (full + scaffold)}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.19\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R7_reconstructed.png}
    \caption*{(e) V7 (full, no scaffold)}
  \end{subfigure}
  \caption[Stage~1 reconstructions across four variants]{%
    \textbf{Stage~1 reconstructions on a held-out gym scene.}
    (a)~The ground-truth scene from SceneSplat-7K shows treadmills, weight benches, a multi-station rack, and a bed with clothing. (b)~V1 (baseline, global encoder and flat decoder) collapses to a single washed-out blob with no recognisable structure. (c)~V3 adds the Hilbert reorder and the position scaffold; some spatial separation appears, but the flat $777$M-parameter decoder still bottlenecks the per-scene context and the output is closer to a smeared cloud than to a room layout. (d, e)~V6 and V7 use the local encoder paired with the token-local decoder ($1.6$M params) and the disentangled split latent. Distinct object clusters become visible and the overall layout of the room is recoverable. V6 (with the anchor scaffold) and V7 (without) are close in both quantitative score and visual quality. The reconstructions are recognisable as ``a room with multiple distinct objects'' but are far from photorealistic; this is the qualitative companion to the V6 validation $\ell_2$ of about $47$.}
  \label{fig:recon}
\end{figure*}

What this figure makes clear is something the quantitative table does not. The V1 to V3 jump on the aggregate metric is large ($99 \to 45$), but the visual difference between V1 and V3 is small: both are essentially incoherent blobs. The visual jump happens between V3 and V6, even though the aggregate metric only changes from $45$ to $47.5$. The reason is that the position component is the dominant term in $\ell_2$, and once it is brought into the same range, the remaining absolute differences in colour, opacity, scale, and quaternion are reflected in the visual quality rather than in the headline number. The qualitative figure is therefore an essential companion to the table, not a redundant one.

I want to be honest here: even the best Stage~1 reconstruction (V6) is not a publishable scene-level reconstruction in the sense of NeRF or 3DGS papers. It is a recognisable arrangement of object-like clusters in approximately the right positions. The thesis demonstrates that the architectural changes I propose move the system from a complete-failure regime (V1) to a regime where the scene structure becomes visible (V6), but the system is not yet at a quality that would compete with per-scene 3DGS reconstructions or with the original Can3Tok demonstrations at the 300-scene scale. The dataset shift (joint splat fitting in Can3Tok versus frozen pre-fitted splats in SceneSplat-7K) is, in my reading, the dominant residual obstacle, and I return to this in Chapter~\ref{chap:discussion}.

%% ──────────────────────────────────────────────────────────────────────────
\section{Per-Gaussian Feature Visualisation}
\label{sec:results_pca}

Figure~\ref{fig:pca} visualises the per-Gaussian feature structure of V6 and V7 on the same held-out scene. For each variant I show three top-down views. The leftmost column shows a PCA of the input Gaussian features (the 14-dim vectors fed to the encoder) projected to three components and mapped to RGB. The middle column shows the same PCA basis applied to the Gaussian features reconstructed by the decoder. The rightmost column shows a PCA of the per-Gaussian InfoNCE projection $f_i$ (Eq.~\eqref{eq:infonce}) on the labelled Gaussians only.

\begin{figure*}[t]
  \centering
  \begin{subfigure}[t]{0.31\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R6_pca_visu_input.jpg}
    \caption*{(a) V6: input PCA}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.31\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R6_pca_visu_reconstructed.jpg}
    \caption*{(b) V6: reconstructed PCA}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.31\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R6_pca_visu_semantic_infonce.jpg}
    \caption*{(c) V6: per-Gaussian InfoNCE PCA}
  \end{subfigure}
  \\[0.4em]
  \begin{subfigure}[t]{0.31\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R7_pca_visu_input.jpg}
    \caption*{(d) V7: input PCA}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.31\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R7_pca_visu_reconstructed.jpg}
    \caption*{(e) V7: reconstructed PCA}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.31\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/R7_pca_visu_semantic_infonce.jpg}
    \caption*{(f) V7: per-Gaussian InfoNCE PCA}
  \end{subfigure}
  \caption[Per-Gaussian feature PCA visualisations]{%
    \textbf{Per-Gaussian feature structure on a held-out scene.}
    Top row: V6 (with position scaffold). Bottom row: V7 (without).
    \emph{Left column:} a 3-component PCA of the input Gaussian features (14-dim) mapped to RGB. Spatially adjacent points receive consistent colours; each piece of furniture forms a coherent colour patch.
    \emph{Middle column:} the same PCA basis applied to the reconstructed Gaussians. The mapping from spatial region to colour is preserved, which shows that the latent retains per-primitive feature identity, not just bulk geometry. V6 is denser; V7 is sparser but still preserves the colour-to-region correspondence.
    \emph{Right column:} a PCA of the per-Gaussian InfoNCE projection $f_i \in \mathbb{R}^{32}$ on labelled Gaussians only (unlabelled Gaussians from ARKitScenes and ScanNet++ chunks are not shown). Distinct colour clusters per ScanNet-72 category are visible, indicating that the auxiliary contrastive loss creates semantically structured features without disrupting reconstruction. This is the empirical evidence behind the RQ3 answer that per-Gaussian semantic supervision is compatible with scene-level reconstruction.}
  \label{fig:pca}
\end{figure*}

The middle column is the most diagnostic. If the autoencoder were collapsing to a single mode, the reconstructed PCA would assign nearly uniform colour across the scene. Instead, each object cluster retains a colour that matches the input PCA. The InfoNCE projection in the right column shows the same scene with only the labelled Gaussians visible; the per-category clustering is clean and matches the floor-plan structure of the input. Comparing V6 and V7, the V6 reconstruction is denser (every region has some reconstructed Gaussian) while V7 has more gaps. This is consistent with the position scaffold giving V6 a more reliable per-token anchor.

%% ──────────────────────────────────────────────────────────────────────────
\section{Stage~2 Generative Results}
\label{sec:results_stage2}

Six Stage~2 training jobs were run, covering generation and completion on the V5, V6, and V7 latents. Table~\ref{tab:stage2_summary} summarises the best velocity loss reached by each.

\begin{table}[h]
  \centering
  \caption[Stage 2 training summary]{\textbf{Stage~2 training summary across six jobs.} Best velocity loss is the lowest reported validation loss across training; the corresponding epoch indicates when the minimum was hit. Layout and geometry losses are not directly comparable to completion loss because they are computed on different latent shapes ($16 \times 32$ and $512 \times 32$ versus the masked completion target). All jobs ran for 500 epochs.}
  \label{tab:stage2_summary}
  {\small
  \begin{tabular}{llrr}
    \toprule
    Stage & Variant & Best val.\ loss ($\downarrow$) & Best epoch \\
    \midrule
    completion & V5  & 32.48  & 300 \\
    layout     & V6  & 0.135  & 499 \\
    geometry   & V6  & 0.037  & 150 \\
    completion & V6  & 32.16  & 450 \\
    layout     & V7  & 0.487  & 200 \\
    geometry   & V7  & 0.075  & 450 \\
    completion & V7  & 32.23  & 350 \\
    \bottomrule
  \end{tabular}}
\end{table}

The layout and geometry losses for V6 (0.135 and 0.037) sit at the low end of what flow-matching training produces on similar latent shapes; the comparable V7 losses are higher (0.487 and 0.075), which I read as the absence of the position scaffold in V7's Stage~1 making the geometry latent harder for the DiT to model. The completion losses across V5, V6, and V7 all sit around $32.2$, dominated by the position component that Stage~2 inherits from the Stage~1 decoder output range.

\paragraph{Flow matching is learning velocity well.} Inspecting the per-batch flow diagnostics across all six runs, the cosine similarity between predicted and target velocity vectors stabilises above $0.96$ for V6 and V7 geometry, above $0.95$ for V6 layout, and around $0.75$ for V7 layout. These numbers indicate that the DiTs themselves are learning the rectified-flow velocity field correctly. The remaining quality limitation comes from the Stage~1 decoder, not from the flow matching.

\paragraph{Position compression.} A consistent finding across all Stage~2 diagnostics is that the positional output of the Stage~1 decoder, when fed Stage~2 latents, lies in a much smaller range than the canonical $\pm 10$\,m training frame. For V6 the geometry decoder outputs positions in roughly $\pm 2$\,m; for V7 they are even more compressed, around $\pm 0.7$\,m. The flow matching reproduces whatever range the encoder produced, so the compression originates in the Stage~1 decoder's output, not in the Stage~2 model. I treat this as a Stage~1 limitation that propagates downstream, and I discuss it further in \S\ref{sec:disc_compression}.

\paragraph{Qualitative Stage~2 generation and completion (V6).} Figure~\ref{fig:stage2_exp6} shows four point clouds from the V6 system: a ground-truth scene, an unconditional generation, a partial masked input, and the corresponding completion. All four are obtained by passing the relevant Stage~1 latent through the frozen Stage~1 decoder, so the comparison is to the Stage~1 reconstruction quality (the level the system actually produces), not to the raw SceneSplat-7K scene. This is the fair comparison given that Stage~1 has not converged to a photorealistic level.

\begin{figure*}[t]
  \centering
  \begin{subfigure}[t]{0.24\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/fig_exp6_gt.png}
    \caption*{(a) Stage 1 reconstruction (``GT'')}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.24\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/fig_exp6_generated.png}
    \caption*{(b) Stage 2 generation}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.24\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/fig_exp6_partial.png}
    \caption*{(c) Partial input}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.24\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/figures/fig_exp6_completion.png}
    \caption*{(d) Stage 2 completion}
  \end{subfigure}
  \caption[Stage~2 V6 qualitative results]{%
    \textbf{Stage~2 generation and completion on the V6 latent.} All four PLYs are produced by the frozen Stage~1 decoder, so the comparison is to what Stage~1 can reconstruct, not to the raw SceneSplat-7K scene. (a)~Stage~1 reconstruction of a held-out scene, used here as the reference. (b)~Unconditional Stage~2 generation: sample $z_s$ from \texttt{LayoutDiT}, sample $z_g$ from \texttt{GeometryDiT512} conditioned on $z_s$, decode through the frozen Stage~1 decoder. The shape of the generated point cloud closely matches the reference. (c)~Partial input: the same held-out scene encoded by the frozen Stage~1 encoder and then masked: $40\%$ of Hilbert tokens are kept clean (observed) and $60\%$ are replaced by noise. (d)~Stage~2 completion: \texttt{CompletionDiT} denoises the masked tokens, conditioned on the observed ones via the layout context $z_s$. The completed point cloud recovers coarse layout but is more diffuse than the reference. Generation reproduces the structural pattern of Stage~1 well; completion is more difficult and shows visible degradation.}
  \label{fig:stage2_exp6}
\end{figure*}

The qualitative reading of Figure~\ref{fig:stage2_exp6} is what matters here. The Stage~2 generator (panel b) produces an output whose shape and extent are close to the Stage~1 reference (panel a). This is the system working as designed: the DiTs learned a velocity field that warps Gaussian noise into the latent distribution that the encoder produces from real scenes, and the decoder turns the result back into Gaussians. The completion task (panel d) is harder. The model has to infer the masked Hilbert blocks from the observed ones, and the result is recognisably scene-shaped but more diffuse than the reference, with the high-density region of the scene spreading along the diagonal. This is the partial-success regime: completion is working but loses sharpness compared to generation.

\paragraph{Why generation is easier than completion in this setup.} Generation samples from the unconditional distribution of the geometry latent given a generated layout code; it does not have to match any particular target. Completion has to denoise the masked positions in a way that is consistent with the observed positions, which is a constrained inverse problem on an already-imperfect latent. The Stage~1 decoder amplifies any residual error in the completed latent because position is the most sensitive output channel (\S\ref{sec:results_components}). This combination is what produces the diffuse completion output.

\paragraph{What is not reported.} I do not report rendered-image quality metrics (PSNR, SSIM, LPIPS) for either Stage~1 reconstructions or Stage~2 outputs. The qualitative reading above is honest about the limits of the present system, but a perceptual evaluation against rendered ground truth is the natural next experiment.